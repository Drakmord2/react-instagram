import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    footerIcon: {
        fontSize:22,
        marginRight:10
    },
    footerIconRed: {
        fontSize:22,
        marginRight:10,
        color: '#d00'
    }
});
